export const metadata = {
  title: 'Staff Dashboard — Glowvie',
  robots: 'noindex, nofollow',
};

export default function StaffLayout({ children }) {
  return <div className="text-black">{children}</div>;
}
